import serpiente.*
import wollok.game.*
import juego.*

program SerpienteGame {
	keyboard.r().onPressDo({music.play()})
	game.title("Serpiente")
	game.height(10)
	game.width(22)
	game.boardGround("escenarioLimpio.png")
	game.schedule(7000, { game.say(serpiente, "¡Hora de comer! Presione Enter para ver instrucciones.") })
	nivel1.iniciar()
	game.cellSize(64)
	game.start()
}
